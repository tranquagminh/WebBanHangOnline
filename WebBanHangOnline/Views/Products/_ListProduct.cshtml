@model IEnumerable<WebBanHangOnline.Models.EF.Product>


@if (Model != null && Model.Any())
{
    foreach (var item in Model)
    {
        <div class="product-item @item.ProductCategory.Alias">
            @if (item.isSale )
            {
                <div class="product discount product_filter">
                    <div class="product_image">
                        <img src="@item.Image" alt="Product_@item.Id">
                    </div>
                    <div class="favorite favorite_left"></div>
                    <div class="product_bubble product_bubble_right product_bubble_red d-flex flex-column align-items-center"><span>Sale</span></div>
                    <div class="product_info">
                        <h6 class="product_name"><a href="#">@item.Title</a></h6>
                        @if (item.PriceSale > 0)
                        {
                            <div class="product_price">@WebBanHangOnline.Common.Common.FormatNumber(item.Price, 0)<span>@WebBanHangOnline.Common.Common.FormatNumber(item.PriceSale, 0)</span></div>
                        }
                        else
                        {
                            <div class="product_price">@WebBanHangOnline.Common.Common.FormatNumber(item.Price, 0)</div>
                        }

                    </div>
                </div>
            }
            else
            {
        <div class="product product_filter">
            <div class="product_image">
                <img src="@item.Image" alt="Product_@item.Id">
            </div>
            <div class="favorite"></div>
            @if (item.isFeature)
            {

                <div class="product_bubble product_bubble_left product_bubble_green d-flex flex-column align-items-center"><span>new</span></div>
            }
            
            <div class="product_info">
                <h6 class="product_name"><a href="#">@item.Title</a></h6>
                <div class="product_price">@WebBanHangOnline.Common.Common.FormatNumber(item.Price, 0)</div>
            </div>
        </div>
            }

            <div class="red_button add_to_cart_button"><a href="#">add to cart</a></div>
        </div>
    }

}
